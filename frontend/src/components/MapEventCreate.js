import React from "react";
import { Button, Form, FormGroup, Label, Input } from "reactstrap";

const MapEventCreate = (props) => {
  return <>Create Event Route</>;
};

export default MapEventCreate;
